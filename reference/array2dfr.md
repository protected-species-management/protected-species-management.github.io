# Cast array to data frame

Cast array to data frame

## Usage

``` r
array2dfr(object, value_to = "value", dim.names = list())
```

## Arguments

- object:

  `array` class object

- value_to:

  column header for array value

- dim.names:

  list of dimension names
